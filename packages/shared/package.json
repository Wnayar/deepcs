{
  "name": "@deepcs/shared",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "//": "Subpath exports, deliberately no barrel — see the note at the top of src/service.ts.",
  "exports": {
    "./service": "./src/service.ts",
    "./logger": "./src/logger.ts",
    "./services": "./src/services.ts",
    "./db": "./src/db.ts",
    "./redis": "./src/redis.ts",
    "./headers": "./src/headers.ts",
    "./events": "./src/events.ts"
  },
  "scripts": {
    "build": "echo 'consumed from source; bundled into each service by tsup' && exit 0",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "fastify": "catalog:",
    "ioredis": "catalog:",
    "pg": "catalog:",
    "pino": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "@types/pg": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
