{
  "name": "@deepcs/shared",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "//": "Subpath exports, deliberately no barrel — see src/service.ts and the phase-0 notes.",
  "exports": {
    "./service": "./src/service.ts",
    "./logger": "./src/logger.ts",
    "./services": "./src/services.ts",
    "./db": "./src/db.ts",
    "./redis": "./src/redis.ts",
    "./headers": "./src/headers.ts"
  },
  "scripts": {
    "build": "echo 'consumed from source; bundled into each service by tsup' && exit 0",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "fastify": "catalog:",
    "ioredis": "catalog:",
    "pg": "catalog:",
    "pino": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "@types/pg": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
