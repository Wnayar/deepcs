node_modules/
dist/
.env
.env.local
# Catch every other .env variant; .env.example is the one committed template.
.env.*
!.env.example
# Credential material. serviceAccountKey.json is the default filename the GCP
# console gives a downloaded service account key — the realistic phase 6 risk.
*.pem
*.key
serviceAccountKey.json
*.log
coverage/
.DS_Store
.firebase/
firebase-debug.log
ui-debug.log
.turbo/
*.tsbuildinfo
