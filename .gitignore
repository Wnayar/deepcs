node_modules/
dist/
.env
.env.local
# Catch every other .env variant; .env.example is the one committed template.
.env.*
!.env.example
# Credential material, in case any is ever added. Nothing local needs a
# certificate or a private key today.
*.pem
*.key
*.log
coverage/
.DS_Store
# The Auth emulator's local state and logs.
.firebase/
firebase-debug.log
ui-debug.log
.turbo/
*.tsbuildinfo
# Personal interview prep for this repo, kept alongside it but not part of it.
interview-prep-guideline.md
