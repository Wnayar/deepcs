dist/
node_modules/
pnpm-lock.yaml

# Prose. Prettier pads every markdown table cell to align columns, so editing
# one row rewrites the whole block and the diff stops being readable. That
# covers the design doc, the docs tree, and the lesson fixtures.
docs/
content/
CLAUDE.md
DESIGN.md
README.md
