dist/
node_modules/
pnpm-lock.yaml

# Prose. Prettier pads every markdown table cell to align columns, so editing
# one row rewrites the whole block and the diff stops being readable. Same
# reason DESIGN.md was excluded from the start.
DESIGN.md
docs/
CLAUDE.md
