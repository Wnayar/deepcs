{
  "name": "@deepcs/web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "//": "The one package here that is not a Node service. It builds to a static bundle rather than running a server, so it has no Dockerfile target, no port and no /health/ready.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@fontsource-variable/inter": "catalog:",
    "firebase": "catalog:",
    "lib0": "catalog:",
    "marked": "catalog:",
    "monaco-editor": "catalog:",
    "prismjs": "catalog:",
    "react": "catalog:",
    "react-dom": "catalog:",
    "react-router": "catalog:",
    "y-monaco": "catalog:",
    "y-protocols": "catalog:",
    "yjs": "catalog:"
  },
  "devDependencies": {
    "@types/prismjs": "catalog:",
    "@types/react": "catalog:",
    "@types/react-dom": "catalog:",
    "@vitejs/plugin-react": "catalog:",
    "typescript": "catalog:",
    "vite": "catalog:",
    "vitest": "catalog:"
  }
}
